# Test Environment Configuration
NODE_ENV=test

# Database Configuration (Test Schema)
DB_HOST=localhost
DB_PORT=5434
DB_NAME=hollon
DB_USER=hollon
DB_PASSWORD=hollon_dev_password
DB_SCHEMA=hollon_test

# Logging
LOG_LEVEL=error

# Test Configuration
TEST_TIMEOUT=30000

# Claude API (Mock in tests)
CLAUDE_API_KEY=test-key-not-used
